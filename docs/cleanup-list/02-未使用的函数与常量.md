# 02 — 未使用的函数与常量

> 说明：以下代码经静态分析确认未被任何位置引用。全部已清理完成。
> 最后更新：2026-05-16（已完成全部清理）

---

## 2.1 已完成清理的函数/方法

### [x] ✅ `internal/processor/pipeline.go` — `removeAdContent`

**已修复**：从空实现改为实际正则替换。同时也为 6 处被忽略的数据库错误添加了 error 日志。

### [x] ✅ `internal/processor/newline_fixer.go`

| 函数 | 行号 | 处理 |
|------|------|------|
| `skipSpaces` | 256 | **保留** — 经确认在文件内部被 7 次调用 |
| `FixWithPosition` | 422-464 | **已删除** |
| `DetectParagraphBoundaries` | 466-478 | **已删除** |
| `MaxParagraphLength` (struct field) | 26 | **已删除** （从未被读取） |

### [x] ✅ `internal/processor/processor.go`

| 函数/变量 | 行号 | 处理 |
|------|------|------|
| `reviewManager` (global) | 31 | **已删除** |
| `ProcessWithReview` | 132-144 | **已删除** |
| `GetSuggestions` | 147-150 | **已删除** |
| `GetReviewManager` | 225-227 | **已删除** |
| `"voidtext/internal/review/manager"` (import) | 10 | **已删除** |

同时删除了整个 `internal/review/manager/` 包。

### [x] ✅ `internal/processor/state_manager.go`

全部 6 个未使用的方法已删除，其调用者已改为直接调用 `database.*` 函数：

| 函数 | 处理 |
|------|------|
| `UpdateFileStatusWithLog` | 已删除 |
| `UpdateFileStatusSimple` | 已删除 |
| `LogProcessingStep` | 已删除 |
| `LogProcessingError` | 已删除 |
| `LogProcessingSuccess` | 已删除 |
| `UpdateStatusAndLog` | 已删除 |
| `marshalDetails` | 已删除 |
| `"encoding/json"` import | 已删除（不再使用） |

`StartProcessingStep` / `CompleteProcessingStep` / `FailProcessingStep` / `SkipProcessingStep` 改为直接调用 `database.UpdateFileStatusWithLog`。

### [x] ✅ `internal/processor/model_repairer.go`

- `min()` (L634): **已删除** — 由 Go 1.21+ 内置 `min` 替代
- `max()` (L1510): **已删除** — 由 Go 1.21+ 内置 `max` 替代

### [x] ✅ `internal/external/api.go`

- `min()` (L892): **已删除** — 由内置 `min` 替代

### [x] ✅ `internal/external/api.go` — 限流器死代码

全部已删除（~109 行）：
- `APIRateLimiter` 结构体 + `NewAPIRateLimiter` + `Acquire`
- 全局变量 `remoteRateLimiter` / `localRateLimiter`
- `RateLimiterConfig` + `DefaultRateLimiterConfig`
- `initRateLimiters` / `getRemoteRateLimiter` / `getLocalRateLimiter`
- `doRequestWithRetry` 中的 `Acquire()` 调用点

### [x] ✅ `internal/logging/logger.go`

已删除函数（~80 行）：
- `Debugf` / `Infof` / `Warnf` / `Errorf` / `Fatalf`
- `ProcessingDebug/Info/Warn/Error`
- `DatabaseDebug/Info/Warn/Error`
- `APIDebug/Info/Warn/Error` — 注意：调用者 `files.go` 和 `rate_limit.go` 已改为使用基础 `logging.Warn/Error/Info`
- `FileDebug/Info/Warn/Error` — 调用者同上

---

## 2.2 已修复的常量与变量

| 文件 | 处理 |
|------|------|
| `database/db.go` — `TableChunkRepairCache` | 已删除重复常量，2 处引用改为 `ChunkRepairCacheTable` |
| `database/chunk_cache_repo.go` — `StatsTimeWindow` | 已改为 unexported `statsTimeWindow` |
| `database/chunk_cache_repo.go` — `DefaultCacheRetentionDays` | 已改为 unexported `defaultCacheRetentionDays` |

---

## 2.3 汇总

| 类别 | 清理项数 | 状态 |
|------|---------|------|
| 删除的函数/方法 | ~28 | ✅ |
| 删除的全局变量 | 3 | ✅ |
| 统一常量 | 3 | ✅ |
| go build | ✅ 通过 | — |
| go vet | ✅ 通过 | — |
