# 核心准则 (Surgical & Simple)

- 先思后行：需求模糊必先询问。优先选最简方案，拒绝过度设计。
- 手术刀修改：仅动必要代码。禁止无关重构，必须匹配现有风格。
- 极简清理：能短不长。仅清理因本次改动产生的废弃代码（Import/变量）。
- 目标驱动：流程：编写复现测试 -> 修复/实现 -> 验证通过。

# 执行标准

## 交互
- 全中文回复。技术回答须含：原理 + 代码示例 + 边界说明。

## 格式
- 2空格缩进，K&R风格大括号，行宽 ≤ 120。

## 命名
- camelCase: 变量/函数 (布尔必带 is/has/can)。
- PascalCase: 类/组件/类型。
- UPPER_SNAKE_CASE: 常量。
- snake_case: 数据库字段。
- kebab-case: 文件名。

## 质量
- 严格类型：禁止 any。异步必用 async/await。
- 职责单一：函数 ≤ 80行。禁止魔法值，错误须结构化捕获。
- 注释：公共API用 JSDoc/TSDoc。TODO须带 // TODO(姓名/日期):。

## 技术栈
- 前端：组件与文件名一致，Hooks 必用 use 开头，CSS 禁止全局污染。
- 后端：返回格式 { code, message, data }。日志含 traceId。

## 维护
- 配置与代码分离，严禁硬编码。测试名：should_行为_条件。

# AI 约束

- 需求模糊不盲猜。重构须说明改动点、收益与风险。

