# 调试记录 2026-04-24

## 问题概述

### 问题1：文件上传后直接显示 80% 进度

**现象**：
- 重置文件状态后重新运行处理，进度直接显示 80%
- 当前步骤显示 `finalizing`
- 处理日志只有一条 `review: start`

**根本原因**：
- 状态管理器从 JSON 文件恢复了旧的处理状态
- 状态文件路径：`data/states/{file_md5}_state.json`
- 即使重置了数据库，状态文件仍然存在

**解决方案**：
- 重置脚本需要同时删除状态文件和数据库记录
- 已更新 `full_reset.py` 脚本

**状态**：✅ 已解决

---

### 问题2：本地模型置信度过低

**现象**：
- 本地模型处理了 4-6 个 chunks
- 置信度普遍很低：0.0187、0.0617、0.1618、0.1855、0.2295、0.2919、0.3455
- 平均置信度约 0.1156 - 0.1887
- 远低于阈值（0.7），导致结果被拒绝

**排查过程**：

#### 2.1 检查文本差异
- 数据库中原始文本和修复文本**字节完全相同**
- 长度相同（1037、799、1000、1050 等）
- 但置信度计算结果很低

#### 2.2 检查置信度计算代码
文件：`internal/processor/model_repairer.go`

```go
func (wp *WorkerPool) calculateConfidence(original, repaired string) float64 {
    if original == repaired {
        return DefaultConfidenceConfig.MaxConfidence  // 应该返回 1.0
    }
    // ...
}
```

**问题**：如果文本相同，应该返回 1.0，但实际返回很低

#### 2.3 检查存储到数据库的代码
文件：`internal/processor/model_repairer.go` 第 934-946 行

```go
cacheRecordNew := &database.ChunkRepairCacheRecord{
    FileMd5:          task.FileMd5,
    ChunkID:          task.ChunkID,
    ChunkHash:        chunkHash,
    OriginalText:     task.Content,        // 原始内容
    RepairedText:     repairedText,        // 修复后内容
    // ...
    Confidence:       confidence,          // 置信度
    Source:           source,
}
```

**发现**：
- `OriginalText` 和 `RepairedText` 分别存储
- `Confidence` 是在调用 `processWithLocalModel` 时计算的

#### 2.4 检查 processWithLocalModel 函数
文件：`internal/processor/model_repairer.go` 第 1123-1157 行

```go
func (wp *WorkerPool) processWithLocalModel(content string) (string, float64, string) {
    // ...
    repairedText, err := wp.ollamaClient.CorrectText(content)
    // ...
    confidence := wp.calculateConfidence(content, repairedText)
    // ...
    return repairedText, confidence, ""
}
```

**流程**：
1. 调用 `CorrectText(content)` 获取修复文本
2. 调用 `calculateConfidence(content, repairedText)` 计算置信度
3. 返回修复文本和置信度

#### 2.5 检查 CorrectText 函数
文件：`internal/external/ollama.go` 第 244-251 行

```go
func (oc *OllamaClient) CorrectText(text string) (string, error) {
    systemPrompt := `你是一个专业的中文小说校对编辑。请纠正以下文本中的错别字、语法错误和乱码，保持原文的意思不变。只输出修正后的文本，无需解释。`
    userPrompt := "请纠正以下文本中的错别字和语法错误：\n" + text + "\n\n修正后的文本："
    return oc.Generate(userPrompt, systemPrompt)
}
```

**发现**：
- `CorrectText` 调用 `Generate` 函数
- `Generate` 函数返回的文本可能被处理过

#### 2.6 检查 Generate 函数
文件：`internal/external/ollama.go` 第 96-199 行

**关键发现**：
- `Generate` 函数直接返回 `genResp.Response`
- 没有对响应文本做任何处理

---

### 当前假设

**假设1**：`CorrectText` 返回的文本和原始文本在 Go 字符串比较时不相等，但在存储到数据库时变成了相同

可能原因：
1. `CorrectText` 返回的文本包含不可见字符（如空格、换行）
2. 存储到数据库时，文本被规范化或截断
3. 置信度计算时使用的文本和存储到数据库的文本不同

**假设2**：置信度计算代码没有被正确编译或运行

可能原因：
1. 代码修改后没有重新编译
2. 运行的是旧版本的二进制文件

---

## 下一步排查计划

### 1. 验证编译状态
- [ ] 检查二进制文件编译时间
- [ ] 确认运行的是最新编译的版本

### 2. 添加调试日志
- [ ] 在 `calculateConfidence` 函数中添加日志，输出 `original` 和 `repaired` 的长度和前 100 字符
- [ ] 在 `processWithLocalModel` 函数中添加日志，输出 `content` 和 `repairedText` 的比较结果

### 3. 检查文本处理
- [ ] 检查 `CorrectText` 返回的文本是否包含额外字符
- [ ] 检查存储到数据库前是否对文本做了处理

### 4. 测试置信度计算
- [ ] 编写单元测试验证 `calculateConfidence` 函数
- [ ] 测试相同文本、不同文本、部分相同文本的置信度计算

---

## 已完成的修改

### 1. 置信度算法优化
- 从 LCS 算法改为 Levenshtein 编辑距离
- 优化合理性检查逻辑
- 调整权重配置（相似度 0.7，合理性 0.3）

### 2. 重置脚本完善
- 添加状态文件删除逻辑
- 确保 `current_step` 设置为有效步骤名（`cleaning`）

---

## 相关文件

- `internal/processor/model_repairer.go` - 置信度计算核心逻辑
- `internal/external/ollama.go` - Ollama 客户端实现
- `internal/database/chunk_cache_repo.go` - 数据库存储逻辑
- `web/backend/handlers/process.go` - 处理流程控制
- `internal/processor/state_manager.go` - 状态管理器
- `full_reset.py` - 文件状态重置脚本

---

## 备注

- 数据库编码：UTF-8
- 本地模型：`modelscope.cn/Qwen/Qwen2.5-3B-Instruct-GGUF`
- 本地模型超时时间：30 秒
- 修复模型类型：`local`
