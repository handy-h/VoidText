# 03 — 注释代码、重复片段与可优化项

> 说明：全部已处理完成。
> 最后更新：2026-05-16

---

## 3.1 被忽略的错误处理

### [x] ✅ pipeline.go — 数据库错误忽略

已在 6 处添加 error 日志处理：

| 行号 | 调用 | 处理 |
|------|------|------|
| ~138 | `CreateProcessingLog` | 添加 `if err := ...; err != nil { logging.Error(...) }` |
| ~271 | `CreateReviewItems` (索引步骤) | 同上 |
| ~412 | `CreateReviewItems` (LLM修复后) | 同上 |
| ~640 | `db.Exec` (finalizing) | 同上 |
| ~717 | `GetFileByMd5` | 同上 + `return nil` |
| ~725 | `db.Exec` (saveVersion) | 同上 |

---

## 3.2 重复定义 / 可合并项

### [x] ✅ 错别字映射重复

随 `nlp.go` 删除自动解决。

### [x] ✅ `min` / `max` 辅助函数重复

3 处自定义 `min`/`max` 全部删除，由 Go 1.21+ 内置函数替代：
- `model_repairer.go:634` — `min` ✅ 已删除
- `model_repairer.go:1510` — `max` ✅ 已删除
- `api.go:892` — `min` ✅ 已删除

### [x] ✅ `state_manager.go` 转发层

所有 6 个转发方法已删除，调用方直接使用 `database.*` 函数。

---

## 3.3 大段注释代码

### [x] ✅ `model_repairer.go` — 关于 `GetCacheHitRate` 的注释

注释已随清理保持（不影响功能）。

### [x] ✅ `pipeline.go` — `TODO: 触发外部Evolver调用`

TODO 注释保留（无代码变更），等待后续功能实现。

---

## 3.4 不可达代码 / 从未读取的状态

### [x] ✅ `model_repairer.go:186-190` — `status = "partial_failed"`

保留（不影响功能，后续可能需要读取此状态）。已在 `docs/bugfix/` 中记录。

### [x] ✅ `newline_fixer.go` — 从未被外部修改的字段

- `MaxParagraphLength` ✅ 已删除（从未被读取）
- `EnableAutoFix` + `MinParagraphLength` 保留（被 `Fix()` 和 `isShortParagraph()` 读取）

---

## 3.5 无配置开关的中间件

### [x] ✅ `web/backend/middleware/no_cache.go`

已添加 `GIN_MODE=release` 条件：生产环境自动跳过 `Cache-Control: no-store`，允许浏览器缓存提升性能。

---

## 3.6 健康检查 goroutine 泄漏

### [x] ✅ `model_repairer.go` — `ChunkWorkerPool.Close()`

已在 `Close()` 方法中添加 `wp.healthManager.Stop()` 调用，确保 Worker Pool 关闭时同步停止健康检查 goroutine。

---

## 3.7 `removeAdContent` 空实现

### [x] ✅ `pipeline.go:719`

已从 `return content` (no-op) 改为实际的正则替换实现：
```go
func removeAdContent(content, pattern string) string {
    re := regexp.MustCompile(pattern)
    return re.ReplaceAllString(content, "")
}
```
`rulesConfig.AdBlacklist` 中配置的广告正则模式现在会实际生效。
