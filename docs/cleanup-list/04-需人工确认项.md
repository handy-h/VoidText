# 04 — 需人工确认的边界项（已全部决策并处理）

> 最后更新：2026-05-16

---

## 4.1 `cmd/benchmark/main.go`

| 决策 | **C — 移出 `cmd/` 到 `tools/benchmark/`** |
|------|------|
| **状态** | ✅ 已完成 — 文件已移至 `tools/benchmark/main.go`，不影响主二进制构建。 |

---

## 4.2 `internal/review/manager/manager.go`

| 决策 | **A — 删除** |
|------|------|
| **状态** | ✅ 已完成 — 整个 `internal/review/manager/` 包已删除。Pipeline 的 review 步骤使用数据库直接操作。 |
| **联动清理** | `processor.go` 中的 `reviewManager` 全局变量、`ProcessWithReview`、`GetReviewManager` 等一并清理。 |

---

## 4.3 `health_check.go` — goroutine 泄漏

| 决策 | **本次修复** |
|------|------|
| **状态** | ✅ 已完成 — 在 `ChunkWorkerPool.Close()` 中添加了 `wp.healthManager.Stop()` 调用。 |

---

## 4.4 `internal/errors/errors.go` — 错误体系

| 决策 | **A — 删除（但另有情况）** |
|------|------|
| **发现** | 该包实际被 `web/backend/handlers/files.go` 和 `web/backend/middleware/error_handler.go` 使用（之前的文档误判为"未使用"）。 |
| **实际处理** | **暂保留** — 删除需要重构 `files.go` 和 `error_handler.go`，本次清理暂不处理，以标记为后续任务。 |
| **建议** | 后续可统一 handler 错误格式（全部改为 `gin.H{"code":..., "message":..., "data":...}`），届时再删除 `internal/errors`。 |

---

## 4.5 `internal/external/api.go` — 限流器

| 决策 | **(b) 清理** |
|------|------|
| **状态** | ✅ 已完成 — 所有限流器代码（~109 行）已删除。限流功能已被 `RetryConfig` + `ExponentialBackoff` 的后置重试机制（遇到 429 指数退避）替代。 |

---

## 4.6 `web/backend/middleware/no_cache.go` — 生产环境配置

| 决策 | **A — 添加 GIN_MODE==release 条件** |
|------|------|
| **状态** | ✅ 已完成 — 生产环境自动跳过 `Cache-Control: no-store`。 |
