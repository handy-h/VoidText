小说文本清洗工具 - 混合架构改进方案（方向版）
一、当前问题分析
问题	具体表现	根因
API限流严重	频繁触发限流错误	代码中自限流设置为2 RPM，过于保守
完全依赖远程API	网络故障时处理中断	无本地兜底方案
成本较高	每本书约2元	所有请求都走远程API
隐私风险	小说内容上传云端	无本地处理选项

二、改进方向总览
┌─────────────────────────────────────────────────────────┐
│                    改进后的处理流程                        │
├─────────────────────────────────────────────────────────┤
│  1. 缓存命中 → 直接返回（已有，保留）                       │
│  2. 缓存未命中 → 本地Ollama模型处理（新增）                  │
│  3. 本地置信度低或失败 → 远程API兜底（优化限流）              │
└─────────────────────────────────────────────────────────┘

核心原则：本地主力 + 远程备用 + 缓存优先

三、各模块改进方向
3.1 配置模块

目标：支持本地模型的可配置化

改进点：

    新增本地模型开关、API地址、模型名称、超时时间、置信度阈值等配置项

    支持通过环境变量或配置文件动态调整

3.2 API调用模块

目标：解决自限流问题，提高并发能力

改进点：

    将限流间隔从500ms调整为50ms（提高10倍吞吐）

    将令牌桶容量从5调整为20

    适当降低重试次数和退避延迟（因为本地会先处理大部分请求）

3.3 本地模型客户端（新增模块）

目标：集成Ollama，实现本地文本修正

改进点：

    新增Ollama HTTP客户端，调用本地部署的Qwen2.5-7B模型

    设计针对错字清洗优化的提示词模板

    实现简单的置信度评估机制（基于输出长度、是否修改等）

    支持健康检查，自动降级到远程API

推荐模型：qwen2.5:7b-instruct-q4_K_M（量化版，约4.5GB显存）
3.4 WorkerPool模块

目标：实现本地优先、远程兜底的处理策略

改进点：

    在processChunk中增加本地模型调用分支

    本地成功且置信度达标 → 直接使用结果

    本地失败或置信度低 → 降级调用远程API

    记录每次处理来源（本地/远程/缓存），便于后续分析

3.5 提示词管理模块

目标：优化提示词质量，提高修正准确率

改进点：

    优化当前提示词，增加示例和输出格式约束

    针对本地模型和远程API使用不同的提示词模板

    保留Evolver框架，但暂不深度集成（当前场景收益有限）

3.6 进度追踪与日志模块

目标：增加混合模式的观测性

改进点：

    新增日志字段：处理来源（local/remote/cache）、置信度、延迟

    增加本地模型健康检查日志

    增加降级事件统计

四、实施优先级
优先级	改进项	预估工作量	说明
P0	调整API限流参数	5分钟	立即见效，无需新增代码
P0	新增Ollama客户端	1-2小时	核心改动
P0	WorkerPool改造（本地优先）	1小时	核心改动
P1	配置文件扩展	30分钟	支持开关和参数调整
P1	提示词优化	15分钟	放一个txt文件即可
P2	日志增强	30分钟	便于监控和调试
P2	健康检查与自动降级	30分钟	提升稳定性
P3	Evolver深度集成	2小时	可选，收益有限
五、部署依赖
需要额外安装的组件
组件	用途	安装命令
Ollama	本地模型运行框架	curl -fsSL https://ollama.com/install.sh | sh
Qwen2.5-7B模型	文本修正模型	ollama pull qwen2.5:7b-instruct-q4_K_M
硬件要求
配置项	最低要求	你的配置	是否满足
显存	4.5GB	RTX 2060 (6GB)	✅
内存	8GB	24GB	✅
硬盘	10GB	充足	✅
六、预期效果
指标	改进前	改进后
限流问题	频繁触发	基本消除
处理速度	~50 tokens/秒	~80-100 tokens/秒
API调用量	100%	~10-20%（仅兜底）
单本书成本	~2元	~0.2-0.4元
离线可用	否	是（本地模式）
隐私保护	需上传云端	可完全本地
七、风险与应对
风险	概率	应对措施
本地模型效果不如API	中	保留远程API兜底，置信度低时自动切换
本地模型响应慢	低	设置超时（如60秒），超时后降级到远程
Ollama服务不稳定	低	健康检查，自动禁用本地模式
显存不足	低	可换用更小的模型（如1.5B版本）
八、后续可选优化

    模型量化：进一步压缩模型大小，提升推理速度

    批处理优化：多个短文本合并请求，减少调用次数

    动态阈值：根据用户审核反馈，动态调整置信度阈值

    多模型支持：支持配置多个本地模型，按需切换

    GPU加速优化：使用vLLM等框架提升推理吞吐量


